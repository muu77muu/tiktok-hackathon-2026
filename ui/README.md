# Optional React prototype

This directory contains the team's optional product-browsing prototype. It is not imported by `starter.agent.Agent` and is not required for official evaluation.

Use the Python Agent and evaluator instructions in the [repository README](../README.md) for the submitted solution.

To inspect the prototype locally:

```bash
npm ci
npm run dev
```
